
export interface QxWWK6FCHProps {
  /**
   * Variant
   * Friendly names map to internal IDs:
   *   "1" → jRomkw6ru
   *   "2" → yFtuHsG5M
   *   "3" → nYytFoNJG
   *   "4" → MYuT38ADB
   *   "5" → T91eo3CQs
   *   "Closed" → V7pRHFNDi
   *   "Desktop" → HVN2zIKy5
   */
  variant?: '1' | '2' | '3' | '4' | '5' | 'Closed' | 'Desktop' | 'HVN2zIKy5' | 'V7pRHFNDi' | 'jRomkw6ru' | 'yFtuHsG5M' | 'nYytFoNJG' | 'MYuT38ADB' | 'T91eo3CQs';
  /**
   * Line Color — pass as `ZsRw4C2ZW` not `lineColor`.
   * @default "var(--token-3f7ec596-b254-4a95-972d-759015f0834a, rgb(69, 69, 69))"
   */
  ZsRw4C2ZW?: string;
  /**
   * Line Radius — pass as `dpd2DydZ5` not `lineRadius`.
   * @default "2px"
   */
  dpd2DydZ5?: string;
  /**
   * Link Color — pass as `gZO4sX8so` not `linkColor`.
   * @default "rgb(102, 102, 102)"
   */
  gZO4sX8so?: string;
  /**
   * Link Font Size — pass as `wwD5sRpoM` not `linkFontSize`.
   * @default 14
   */
  wwD5sRpoM?: number;
  onwwD5sRpoMChange?: string;
  /**
   * All Links New Tab — pass as `CHAFW3O06` not `allLinksNewTab`.
   * @default false
   */
  CHAFW3O06?: boolean;
  onCHAFW3O06Change?: string;
  /**
   * Title 1 — pass as `KcjQRJUER` not `title1`.
   * @default "Link 1"
   */
  KcjQRJUER?: string;
  onKcjQRJUERChange?: string;
  /**
   * Link 1 — pass as `VnzORp2bA` not `link1`.
   */
  VnzORp2bA?: string;
  /**
   * Title 2 — pass as `HHeRT8CDE` not `title2`.
   * @default "Link 2"
   */
  HHeRT8CDE?: string;
  onHHeRT8CDEChange?: string;
  /**
   * Link 2 — pass as `znPD3gymO` not `link2`.
   */
  znPD3gymO?: string;
  /**
   * Title 3 — pass as `Mc5GaGeDB` not `title3`.
   * @default "Link 3"
   */
  Mc5GaGeDB?: string;
  onMc5GaGeDBChange?: string;
  /**
   * Link 3 — pass as `tA9plyMo9` not `link3`.
   */
  tA9plyMo9?: string;
  /**
   * Title 4 — pass as `L5NLL_jsl` not `title4`.
   * @default "Link 4"
   */
  L5NLL_jsl?: string;
  onL5NLL_jslChange?: string;
  /**
   * Link 4 — pass as `Ih6_QJzX3` not `link4`.
   */
  Ih6_QJzX3?: string;
  /**
   * Title 5 — pass as `ul_kJfOvK` not `title5`.
   * @default "Link 5"
   */
  ul_kJfOvK?: string;
  onul_kJfOvKChange?: string;
  /**
   * Link 5 — pass as `FpvSqxHhS` not `link5`.
   */
  FpvSqxHhS?: string;
  /** Additional properties */
  [key: string]: unknown;
}


